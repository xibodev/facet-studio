# Normalize line endings so the three lanes exchange byte-identical files.
* text=auto eol=lf

# Fixtures are the cross-lane contract: their bytes are compared and digested,
# so a CRLF conversion would change a digest and manufacture false drift.
testdata/fixtures/** -text
*.json -text

*.go text eol=lf
*.md text eol=lf
